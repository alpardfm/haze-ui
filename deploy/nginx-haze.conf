# Add inside the existing alpardfm.my.id server block.

location = /haze {
    return 301 /haze/;
}

location ^~ /haze/ {
    root /var/www/alpardfm.my.id;
    try_files $uri $uri/ /haze/index.html;
}
